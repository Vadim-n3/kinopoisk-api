in: header
name: registration
description: Registration data
required: true
schema:
  type: object
  properties:
    username:
      description: Personal name of the user.
      type: string
    password:
      description: User personal password for authorization.
      type: string
    email:
      description: User email for communication, notification, secondary identification way, etc.
      type: string
